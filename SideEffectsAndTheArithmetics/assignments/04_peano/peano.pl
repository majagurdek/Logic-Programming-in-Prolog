peano_number(X, Y) :- fail.

peano_add(X, Y, Z) :- fail.

peano_times(X, Y, Z) :- fail.

peano_greater_than(X, Y) :- fail.

peano_div(X, Y, Z) :- fail.

peano_mod(X, Y, Z) :- fail.

peano_is_even(X) :- fail.

peano_is_odd(X) :- fail.
